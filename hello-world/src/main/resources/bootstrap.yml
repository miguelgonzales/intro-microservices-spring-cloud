# Spring properties
spring:
  application:
    name: hello-world
  cloud:
    config:
      enabled: true
      uri: http://user:mypassword@localhost:8888
      fail-fast: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9999/eureka/ # Ha de coincidir con lo definido en el Eureka Server